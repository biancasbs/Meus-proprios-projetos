/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // ou 'media' para detectar automaticamente o modo escuro do sistema
  content: ['./src/**/*.{html,js}'],
  theme: {
    extend: {},
  },
  plugins: [],
}
